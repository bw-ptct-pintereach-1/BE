const router = require("express").Router();
const Users = require("../helpers/user-model");
const restrict = require("../auth/restrict-middleware");


// code to edit/configure user info

module.exports = router;